import { TorrentCategoryPipe } from './torrent-category-pipe';

describe('TorrentCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new TorrentCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
